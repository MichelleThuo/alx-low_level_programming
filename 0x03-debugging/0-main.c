#include "main.h"

/**
  * main - Test whether the function postitive_or_negative()
  * gives the correct output when given a case of 0
  * Return: 0 if successfuelly compiled, non-zero otherwise
  */

int main(void)
{
	int i;

	i = 0;
	positive_or_negative(i);

return (0);
}
